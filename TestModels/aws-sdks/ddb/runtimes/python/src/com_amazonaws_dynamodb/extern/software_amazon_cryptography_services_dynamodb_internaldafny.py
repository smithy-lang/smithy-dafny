from Wrappers import Option_None, Option_Some

class default__:
    def RegionMatch(client, region):
        # current_session = client.session.Session()
        # return Option_Some(current_session.region_name)

        # TODO get region!!!!
        return Option_None()