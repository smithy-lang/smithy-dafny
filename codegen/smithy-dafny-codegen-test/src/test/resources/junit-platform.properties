# This can be uncommented to run TestModel tests in parallel.
# It's not enabled by default yet because there's a small chance
# of spurious failures due to a race condition in transitively building
# the common StandardLibrary dependency.
#junit.jupiter.execution.parallel.enabled = true
#junit.jupiter.execution.parallel.mode.default = concurrent